<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlog.diary.sample.SampleMapper">

	<select id="getSampleData" parameterType="com.dlog.diary.sample.SampleVO" resultType="map">
		SELECT #{nm} AS 'name', #{age} AS 'age', '영차영차' AS 'said'
	</select>

</mapper>