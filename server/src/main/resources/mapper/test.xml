<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dlog.diary.test.testMapper">

	<select id="getTestData" parameterType="dlog.diary.test.testVO" resultType="map">
		SELECT #{nm}, #{age}, '영차영차'
	</select>

</mapper>